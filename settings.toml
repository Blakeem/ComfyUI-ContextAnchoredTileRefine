# Settings for the Context-Anchored Tile Refine VL nodes.
#
# YOUR OWN COPY
# Copy this file to settings.user.toml and edit the copy. The nodes read settings.user.toml
# whenever it exists and ignore this file. A node update replaces settings.toml, so an edit
# made here is lost. Nothing ever writes settings.user.toml, so your own copy survives.
#
# PRESETS
# Each [presets.<label>] block adds two options to the vlm_method selector on both VL nodes,
# "vision tokens and captions (<label>)" and "captions (<label>)". The selector follows the
# order of the blocks below, so a preset's two options sit together. Add, rename or remove a
# block to change what the selector offers.
# The selector is built when ComfyUI starts, so a new or renamed preset needs a restart.
# Editing a preset's own wording or numbers applies on the next queued run.
# A mistake in this file is a hard error that names the file and the problem. It hides both
# VL nodes from the menu until it is fixed, and ComfyUI prints the reason to its console.
#
# KEYS PER PRESET
# tile_caption_instruction   What the VL model is asked about each tile's own crop. The
#                            answer becomes that tile's conditioning text.
# tile_caption_max_tokens    Generation budget for that answer. Default 768, max 4096. The
#                            budget also covers the model's hidden reasoning turn, so it
#                            sits far above the visible answer length.
# tile_caption_megapixels    How much of the tile the VL model reads. Default 0.147456,
#                            which is 384 x 384 pixels. Range 0.01 to 2.0. Use 0 for the
#                            tile's own size, capped at 2.0. A larger budget reads finer
#                            detail and makes every caption slower.
# global_style_instruction   What the VL model is asked about the whole image, once per
#                            picture. The answer is placed on top of every tile caption so
#                            that all tiles follow one style description. Use "" to skip it.
# global_style_max_tokens    Generation budget for the style answer. Same rules as
#                            tile_caption_max_tokens.
# global_style_megapixels    How much of the whole image the VL model reads for the style
#                            answer. Same rules as tile_caption_megapixels.
#
# PROMPT RULES
# A prompt needs its own stop condition, such as a length word (succinct), a count ceiling,
# or a fixed region list. Without one the answer runs to max_tokens.

# The default. The tile prompt holds placement and demographics, and the style prompt carries
# one medium to every tile. It tends to read an image as a painting or an illustration.
[presets.artwork]
tile_caption_instruction = "succinct prose containing relative and absolute positions of specific things with object and character identifying demographics."
tile_caption_max_tokens = 768
tile_caption_megapixels = 0.147456
global_style_instruction = "succinct flowing prose of only the overall style and artistic medium and physical medium. No objects or items in the scene."
global_style_max_tokens = 768
global_style_megapixels = 0.147456

# What every caption surface asked before 2026-08-21, with no style caption. EU spelling is
# deliberate, since it measured better than US spelling in A/B testing.
[presets.standard]
tile_caption_instruction = "Describe this image, one short line per part. Start with the overall style, palette and lighting. Then say what fills the left, the centre, the right, the top and the bottom, giving each part its own description with what is there, its colour and what its surface is made of. Name whole objects and count repeated objects as one entry."
tile_caption_max_tokens = 768
tile_caption_megapixels = 0.147456
global_style_instruction = ""
global_style_max_tokens = 768
global_style_megapixels = 0.147456
